version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    branches:
      ignore: gh-pages
    steps:
      - run: yarn run publish-gh-pages

      - run:
          name: Print the Current Time
          command: date